import AIJobsClient from "./AIJobsClient";

export const metadata = {
  title: "AI Job Portal — Find AI & Tech Jobs | AI 360 FUTURE",
  description:
    "AI Job Portal — Browse 1000+ AI & tech jobs updated daily. Remote, hybrid, internships and global roles. Search, filter and apply.",
};

export default function Page() {
  return <AIJobsClient />;
}
